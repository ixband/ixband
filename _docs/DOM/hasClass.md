---
layout: docs
category: DOM
title: hasClass
tags: []
---

{% include docs-contents.html %}

### Example
```js
console.log( $B( '#box' ).hasClass( 'plus' ) );
console.log( $B( '#box' ).hasClass( 'plus', 'on' ) );
console.log( $B( '#box' ).hasClass( ['plus', 'on'] );
```